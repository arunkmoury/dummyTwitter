import React from 'react';
import { View, Text } from 'react-native';

const Compose = () => {
    return (
        <View>
            <Text>Compose</Text>
        </View>
    );
}

export default Compose;